# Copy to `.env` (kept local/private, never committed) and fill in your backend
# API base URL for local development/testing. Set in the build environment or a
# local `.env.production` to override the API used by production builds.
VITE_API_URL=