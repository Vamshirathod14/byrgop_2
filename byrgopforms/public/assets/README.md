# BYRGOP 12th Anniversary — logo placeholders

Drop the real event logos into this folder as the files below. The app already
references them and falls back to a clean text wordmark when a file is missing,
so nothing breaks if they are not present yet.

- `logo-left.png` — left logo block (`/assets/logo-left.png`)
- `logo-right.png` — right logo block (`/assets/logo-right.png`)